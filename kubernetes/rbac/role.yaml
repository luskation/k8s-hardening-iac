apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: dvwa-reader
  namespace: dev

rules:
- apiGroups: [""]
  resources:
    - pods
    - services
  verbs:
    - get
    - list
    - watch